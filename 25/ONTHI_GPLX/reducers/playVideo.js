const initState = {
    
}
export default function playVideo (state = initState, action) {
    switch (action.type) {
        case "":
            return {
                
            }
        case "":
            return {
                ...state,
                isVisiable: false
            }
        default:
            return state;
    }
}
