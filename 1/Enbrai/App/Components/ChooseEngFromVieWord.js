import React, { Component } from "react";
import { View, AsyncStorage, StatusBar, Image } from "react-native";
import { Text, Button, Icon } from "react-native-elements";

const ChooseEngFromVieWord = (props) => {

  return (
    <View style={{ padding: 10, marginTop: 100, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ marginTop: 20, paddingLeft: 30, paddingRight: 30, fontSize: 16 }}>ChooseEngFromVieWord</Text>
    </View>
  );
}
export default ChooseEngFromVieWord