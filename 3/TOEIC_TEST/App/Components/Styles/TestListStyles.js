import { StyleSheet, Dimensions } from 'react-native'

export default StyleSheet.create({
    title: {
        alignContent: 'center',
        fontWeight: 'bold',
        fontSize: 20,
        color: 'white'
    },
    iconLeft: {
        paddingStart: 20,
    },

})