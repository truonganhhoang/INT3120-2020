import * as React from 'react';
import { Text, View, StyleSheet, Button, Image } from 'react-native';

export function Man({route, navigation}) {
    //const { name } = route.props;
        return(
            <View>
                <Text> Màn hình mới nè</Text>
                <Text> Profile 2 SCREEN </Text>
                <Text> Profile 2 SCREEN </Text>
            </View>

        );
}